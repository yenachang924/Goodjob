spring:
  datasource:
    url: ${GOODJOB_DB_URL:jdbc:h2:file:./data/goodjob-local;MODE=MySQL;DATABASE_TO_LOWER=TRUE;AUTO_SERVER=TRUE}
    username: ${GOODJOB_DB_USERNAME:sa}
    password: ${GOODJOB_DB_PASSWORD:}
# H2 is an explicit local learning profile. It does not prove MySQL compatibility.
